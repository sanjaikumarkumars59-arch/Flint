fn add(a: i64, b: i64) -> i64 {
    return a + b
}

fn main() {
    x = 5
    y = 10
    result = add(x, y)
    print(result)

    if result > 10 {
        print(1)
    } else {
        print(0)
    }

    mut count = 0
    while count < 3 {
        print(count)
        count = count + 1
    }
}
